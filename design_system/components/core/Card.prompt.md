Surface card — white fill, hairline border, 16px radius, soft gray-tinted shadow; the default container for content.

```jsx
<Card hover>
  <h4>Proyectos</h4>
  <p>Seguimiento de iniciativas del edificio.</p>
</Card>
```

`hover` adds a 2px lift + deeper shadow. `padding` overrides the inset.
